export enum Resource {
  // Keyword
  All = 'all',

  // Common
  App = 'app',
  User = 'user',
  Asset = 'asset',
  Grant = 'grant',
  Client = 'client',
  Config = 'config',
  Profile = 'profile',
  Session = 'session',
}
